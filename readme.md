# 🧧 TrueMoney Angpao Auto Redeem (Bypass Cloudflare)

สคริปต์ Python สำหรับ **รับซองอั่งเปา TrueMoney Wallet อัตโนมัติ** โดยจุดเด่นของโปรเจกต์นี้คือการเลือกใช้ Library `curl_cffi` เพื่อแก้ปัญหาการติด Cloudflare หรือ WAF ของ TrueMoney ทำให้สามารถยิง Request ได้เหมือนเป็น Browser จริง (Chrome Impersonation)

## ✨ ฟีเจอร์หลัก (Features)
- **Advanced Bypass:** ใช้ `curl_cffi` จำลอง TLS Fingerprint ของ Chrome ทำให้เซิร์ฟเวอร์จับไม่ได้ว่าเป็น Bot
- **Auto Extraction:** แกะรหัส Voucher (`v=...`) จากลิงก์ยาวอัตโนมัติ ไม่ต้องมานั่งตัดคำเอง
- **Detailed Logs:** แสดงสถานะชัดเจน ทั้งยอดเงินที่ได้ ชื่อผู้ส่ง หรือสาเหตุที่รับไม่ได้
- **Async Support:** เขียนด้วย `asyncio` รองรับการทำงานแบบ Asynchronous (เตรียมพร้อมสำหรับยิงหลายซองพร้อมกันในอนาคต)

## 🛠️ การติดตั้ง (Installation)

โปรเจกต์นี้รองรับ **Python 3.7 ขึ้นไป**

1. Clone โปรเจกต์นี้ลงเครื่อง
2. ติดตั้ง Library ที่จำเป็น:

```bash
pip install curl-cffi
```

*(หมายเหตุ: `asyncio` และ `re` เป็น Standard Library ของ Python อยู่แล้ว ไม่ต้องติดตั้งเพิ่ม)*

## ⚙️ การใช้งาน (Usage)

1. เปิดไฟล์ `main.py`
2. แก้ไขส่วน **Configuration** ด้านบนสุดของไฟล์:

```python
# ==========================================
# ส่วนตั้งค่า
# ==========================================
MY_PHONE_NUMBER = "08xxxxxxxx"  # เบอร์ TrueMoney Wallet ของคุณ
TARGET_LINK = "https://gift.truemoney.com/campaign/?v=xxxxxxxx" # ลิงก์ซองที่ต้องการเติม
```

3. รันคำสั่ง:
```bash
python main.py
```

## 📊 ตัวอย่างผลลัพธ์ (Output Example)

### ✅ กรณีสำเร็จ (Success)
เมื่อรับเงินได้ ระบบจะแสดงยอดเงินและชื่อผู้ส่ง:
```text
🔍 Testing redemption for link: https://gift.truemoney.com/...
📱 Phone Number: 08xxxxxxxx
🔹 Extracted Voucher Code: 019ba9b46c3c7...
🚀 Sending request to TrueMoney API (Impersonating Chrome)...
📡 HTTP Status: 200
------------------------------
✅ SUCCESS! รับเงินสำเร็จ
💰 ยอดเงิน: 50.00 บาท
👤 จาก: คุณใจดี แจกเงิน
------------------------------
```

### ❌ กรณีไม่สำเร็จ (Failed)
เช่น ซองหมด หรือลิงก์ผิด:
```text
...
📡 HTTP Status: 200
------------------------------
❌ FAILED: ไม่สำเร็จ (แต่ทะลุ Cloudflare แล้ว)
⚠️ Reason: voucher_out_of_stock (ซองนี้ถูกรับไปหมดแล้ว)
------------------------------
```

## 🚀 แนวทางการต่อยอด (Integration & API)

Code ชุดนี้ถูกออกแบบมาให้เป็น Modular สามารถนำไปดัดแปลงเป็น **Backend Service** หรือ **API** ได้ง่าย โดยมีแนวทางดังนี้:

1. **เปลี่ยนโครงสร้าง:** แปลงฟังก์ชัน `test_redeem` ให้รับค่า `(link, phone)` เป็น Parameter
2. **Return ค่า:** เปลี่ยนจาก `print()` เป็นการ `return dict` เช่น:
   ```python
   return {
       "status": True,
       "amount": 50,
       "owner": "Mr. A"
   }
   ```
3. **เชื่อมต่อ Web Framework:** นำไปใช้ร่วมกับ **FastAPI** หรือ **Flask** เพื่อสร้าง Endpoint ให้โปรแกรมอื่นยิงเข้ามาตัดซองได้ เช่น:
   - `POST /api/redeem`
   - Body: `{"link": "...", "phone": "..."}`

## ⚠️ ข้อสงวนสิทธิ์ (Disclaimer)
Source Code นี้จัดทำขึ้นเพื่อการศึกษา (Educational Purpose) เกี่ยวกับการทำงานของ HTTP Request, TLS Fingerprint และการใช้งาน Library `curl_cffi` เท่านั้น ผู้พัฒนาไม่สนับสนุนให้นำไปใช้ในทางที่ผิดกฎหมายหรือละเมิดข้อตกลงการใช้งานของระบบ
